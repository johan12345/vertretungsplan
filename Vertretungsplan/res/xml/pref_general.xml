<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >

    <CheckBoxPreference
        android:defaultValue="true"
        android:key="example_checkbox"
        android:summary="Zu festen Zeiten den Stundenplan neu abrufen"
        android:title="Automatisch synchronisieren" />

    <!--
         NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it.
    -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="5"
        android:entries="@array/pref_klassenstufe_titles"
        android:entryValues="@array/pref_klassenstufe_values"
        android:key="klassenstufe"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_klassenstufe" />

</PreferenceScreen>