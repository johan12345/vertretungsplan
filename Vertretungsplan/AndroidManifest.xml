<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.johan.vertretungsplan"
    android:versionCode="16"
    android:versionName="2.1.9"
    android:installLocation="internalOnly">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="19" />

    <application
        android:name="VertretungsplanApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Ls" >
        <activity
            android:name="com.johan.vertretungsplan.StartActivity"
            android:label="@string/app_name"
            android:logo="@drawable/ic_action_icon" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name="com.johan.vertretungsplan.background.VertretungsplanWidgetProvider" >
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>
        <receiver android:name="com.johan.vertretungsplan.background.OnBootReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <activity
            android:name="com.johan.vertretungsplan.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName="com.johan.vertretungsplan.StartActivity"
            android:logo="@drawable/ic_action_icon" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.johan.vertretungsplan.StartActivity" />
        </activity>
        
        <activity android:name="org.acra.CrashReportDialog"
	        android:theme="@style/Holo.Theme.Dialog.Light"
	        android:launchMode="singleInstance"
	        android:excludeFromRecents="true"
	        android:finishOnTaskLaunch="true"
	        android:logo="@drawable/ic_action_icon" />

        <service
            android:name="com.johan.vertretungsplan.background.VertretungsplanService"
            android:enabled="true"
            android:exported="true" >
        </service>
        
        <service
            android:name="com.johan.vertretungsplan.background.AutostartService"
            android:enabled="true"
            android:exported="true" >
        </service>
    </application>

</manifest>